import 'package:flutter/material.dart';

class Unuthorized extends StatelessWidget {
  const Unuthorized({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Virak single on unauthorized")),
    );
  }
}
